/// <reference types="react" />
declare type Props = {
    id?: string;
    avatarURL: string;
    userName: string;
    commentKind: 'comment' | 'question' | 'bug';
    date?: Date;
    comment: string;
    privateComment?: boolean;
    resolved?: boolean;
    onRemove?: Function;
    onResolve?: Function;
};
export default function Comment({ avatarURL, userName, commentKind, comment, date, privateComment, resolved, onRemove, onResolve, }: Props): JSX.Element;
export {};
