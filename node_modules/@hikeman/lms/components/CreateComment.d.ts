/// <reference types="react" />
declare type Props = {
    avatarURL: string;
    userName: string;
    submitComment: Function;
};
export default function CreateComment({ avatarURL, userName, submitComment, }: Props): JSX.Element;
export {};
